#include "hw.h"
#include <iostream>

using namespace std;

int main() {
	cout << "Power-up" << endl;
	platformInitX();
	cout << "HW initialization done." << endl;
	int val = 0xA;
	while(1) {
		setLed(val);
		val = ~val;
		cout << "here" << endl;
		delayMs(500);
	}
	return 0;
}
